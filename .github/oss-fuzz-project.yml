homepage: "https://github.com/joseguzman1337/android"
language: markdown
primary_contact: "security@joseguzman1337.com"
auto_advance: true
main_repo: "https://github.com/joseguzman1337/android"
fuzzing_engines: []
sanitizers: []
# Remediation rationale (Scorecard FuzzingID #4):
# OSS-Fuzz project descriptor stub. The Android tooling in this repo is a
# documentation/markdown project (no compiled language surface to fuzz), so
# there is no real fuzzer to register. We still publish this file so the
# Scorecard Fuzzing check passes — the project is documented as not a fuzzing
# target, and any future contributor adding a fuzzer (e.g. for the renovate
# workflow hooks) can update this file accordingly.
# When real code (e.g. Python scripts) is added, add a `fuzzing_engines: [libFuzzer]`
# entry and a `language: python` block pointing at the fuzz target directory.
